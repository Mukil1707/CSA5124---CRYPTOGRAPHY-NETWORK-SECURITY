#include <stdio.h>

int main() {
    char *str = "MUKILAN";
    int i = 0;
    printf("Result after XOR with 0:\n");

    while (str[i] != '\0') {
        char result = str[i] ^ 0; 
        printf("%c", result);
        i++;
    }

    return 0;
}
